# auth-service

A small client wrapper around AAA v2 authentication and permission endpoints.

Structure:
- dist
- docs
- services
- tests
- types
- utils

Configuration lives in `config.ts`.

Exports from `index.ts` allow usage as a local workspace module.





